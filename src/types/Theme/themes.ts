export type Theme = {
  fontFamily: string;
  bgColor: string;
  fontColor: string;
  borderColor: string;
  primaryColor: string;
  secondaryColor: string;
  classroomFontColor: string;
  classroomBgColor: {
    fredry: string;
    bukowska: string;
    wf: string;
  };
  boxShadow: string;
  boxShadowInset: string;
};
